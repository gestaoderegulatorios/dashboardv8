# Temp / scratch files
_temp_*.js
_temp_*.html
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Claude local settings (keep launch.json shared)
.claude/settings.local.json
.claude/worktrees/

# Node (V8 will use it)
node_modules/
dist/
*.log

# Python
__pycache__/
*.pyc
.venv/

# ETL — dados brutos (XLSX da equipe) e saída do pipeline
# Não versionar: dados podem ser sensíveis e XLSX incham o repo.
# Apenas o snapshot.json copiado para dashboard-v8/public/data/ vai pro git.
dados_raw/
etl_v8/output/
etl_v8/.venv/
erros_etl.json
validation_report.json

# Claude scheduled tasks lock (local only)
.claude/scheduled_tasks.lock
